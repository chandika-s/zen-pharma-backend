name: CI/CD — auth-service

# Triggers on push to develop / release branches only.
# Feature branches and PRs → ci-pr-auth-service.yml
# PROD promotion → promote-prod.yml (workflow_dispatch)

on:
  push:
    branches: [develop, 'release/**']
    paths:
      - 'auth-service/**'
      - '.github/workflows/ci-auth-service.yml'

permissions:
  id-token: write
  contents: read
  security-events: write

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: auth-service
  GITOPS_REPO: ravdy/zen-gitops

jobs:

  build:
    name: Build & Security Gates
    uses: ./.github/workflows/_java-build.yml
    with:
      service-name: auth-service
      service-dir: auth-service
      ecr-repository: auth-service
      aws-region: us-east-1
      needs-database: true
    secrets: inherit

  deploy-dev:
    name: Deploy — DEV
    needs: build
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - uses: actions/checkout@v4

      - name: Checkout GitOps repo
        uses: actions/checkout@v4
        with:
          repository: ${{ env.GITOPS_REPO }}
          token: ${{ secrets.GITOPS_TOKEN }}
          path: _gitops

      - name: Update image tag — DEV
        run: |
          yq e '.image.tag = "${{ needs.build.outputs.image-tag }}"' -i \
            "_gitops/envs/dev/values-auth-service.yaml"
          cd _gitops
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --staged --quiet && echo "Already up to date" && exit 0
          git commit -m "ci(dev): update auth-service → ${{ needs.build.outputs.image-tag }}"
          git push

  open-qa-pr:
    name: Open PR — QA Promotion
    needs: [build, deploy-dev]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Checkout GitOps repo
        uses: actions/checkout@v4
        with:
          repository: ${{ env.GITOPS_REPO }}
          token: ${{ secrets.GITOPS_TOKEN }}
          path: _gitops

      - name: Create branch, commit, open PR
        env:
          GH_TOKEN: ${{ secrets.GITOPS_TOKEN }}
        run: |
          IMAGE_TAG="${{ needs.build.outputs.image-tag }}"
          VALUES="_gitops/envs/qa/values-auth-service.yaml"
          BRANCH="promote/qa/auth-service/${IMAGE_TAG}"

          if [[ ! -f "$VALUES" ]]; then
            echo "::warning::$VALUES not found — QA PR skipped."
            exit 0
          fi

          cd _gitops
          git checkout -b "$BRANCH"
          yq e ".image.tag = \"${IMAGE_TAG}\"" -i "envs/qa/values-auth-service.yaml"
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          if git diff --staged --quiet; then
            echo "QA already has this image — no PR needed"
            exit 0
          fi
          git commit -m "promote(qa): auth-service → ${IMAGE_TAG}"
          git push origin "$BRANCH"

          printf '%s\n' \
            "## QA Promotion" \
            "" \
            "Service : auth-service" \
            "Image   : ${IMAGE_TAG}" \
            "Branch  : ${{ github.ref_name }}" \
            "Actor   : ${{ github.actor }}" \
            "Build   : ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            "" \
            "## Checklist" \
            "- [ ] DEV smoke test passed (see build link above)" \
            "- [ ] No unexpected config changes in this diff" \
            "- [ ] QA team sign-off" \
            "" \
            "Merging this PR updates envs/qa/values-auth-service.yaml." \
            "ArgoCD (pharma-qa) auto-syncs on merge." > /tmp/pr-body.md

          gh pr create \
            --repo "${{ env.GITOPS_REPO }}" \
            --title "promote(qa): auth-service → ${IMAGE_TAG}" \
            --body-file /tmp/pr-body.md \
            --base main \
            --head "$BRANCH"

          echo "::notice title=QA PR opened::Review and merge in ravdy/zen-gitops to deploy to QA."
